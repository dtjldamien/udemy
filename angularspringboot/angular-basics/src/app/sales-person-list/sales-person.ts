export class SalesPerson {
  // public accessors (no need for get/set methods)
  // parameter properties (no need to declare attributes)
  constructor(
    public firstName: string,
    public lastName: string,
    public email: string,
    public salesVolume: number
  ) {}
}
