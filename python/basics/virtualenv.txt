1. Go to terminal, pip3 install virtualenv
2. Go to project folder, type virtualenv "env_name"
3. Type in terminal, source env_name/bin/activate

Packages: PyPI -> packages that can be installed using pip
pip3 install package_name (django, requests etc)